
import type { SystemStyleObject } from "@mui/system";
import type {Theme} from "@mui/material";

export const borderStyles: SystemStyleObject<Theme> = {
    border: '2px solid',
    borderColor: 'primary.main',
    borderRadius: '5px',
};
